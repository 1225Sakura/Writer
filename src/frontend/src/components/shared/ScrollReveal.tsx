/**
 * ScrollReveal - Scroll-triggered reveal animation components
 *
 * Re-exports from sub-components for backward compatibility.
 */

export { ScrollReveal, ScrollRevealStagger } from './RevealTrigger'
export { ScrollRevealGroup, useReducedMotion, animationStyles, easeOutSmooth } from './RevealContainer'
export type { RevealAnimation } from './RevealContainer'
